{
    "name": "PythonProject1",
    "dockerComposeFile": "../docker-compose.yml",
    "service": "app",
    "workspaceFolder": "/app",
    "customizations": {
        "codespaces": {
            "openPorts": [8000]
        }
    },

    "postStartCommand": "docker-compose up -d"
}
